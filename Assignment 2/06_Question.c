// Write a program which takes a character as an input and displays its ASCII code.

#include<stdio.h>
int main()
{
    char a;
    printf("Enter an a character: ");
    scanf("%c",&a);

    printf("\nThe ASCII code of the character is: %d",a);
}