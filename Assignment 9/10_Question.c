// 10. C program to find all roots of a quadratic equation using switch case
#include <stdio.h>
#include<stdlib.h>
#include<math.h>
int main()
{
    double a, b, c, D;
    // printf("\n\n\tQuadratic equation:\n     example : a^2 + b + c = 0\n\n\nEnter the value of a: ");
    // scanf("%f", &a);
    // printf("Enter the value of b: ");
    // scanf("%f", &b);
    // printf("Enter the value of c: ");
    // scanf("%f", &c);

    D = (b * b) - 4 * (a) * (c);
    double root1 = sqrt(12);
    printf("%f",root1);

    return 0;
}